  public String toString() {
    return new ToStringBuilder(BindingImpl.class)
        .add("key", key)
        .add("source", source)
        .add("provider", internalFactory)
        .toString();
  }

