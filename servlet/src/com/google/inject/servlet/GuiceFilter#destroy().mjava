  public void destroy() {

    try {
      // Destroy all registered filters & servlets in that order
      pipeline.destroyPipeline();

    } finally {
      reset();
      servletContext.clear();
    }
  }

